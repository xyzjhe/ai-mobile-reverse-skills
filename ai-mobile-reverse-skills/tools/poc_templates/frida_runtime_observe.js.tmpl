/*
 * Minimal authorized-only runtime observation template.
 *
 * Replace PLACEHOLDER_* values before use.
 * Intended for observing inputs/outputs, not for destructive actions.
 */

function logDivider() {
    console.log("=======================================================");
}

function safeString(value) {
    if (value === null || value === undefined) return "null";
    return value.toString();
}

Java.perform(function () {
    var TargetClass = Java.use("PLACEHOLDER_CLASS");
    var overload = TargetClass["PLACEHOLDER_METHOD"].overload("java.lang.String");

    overload.implementation = function (arg0) {
        console.log("[*] Hook hit: PLACEHOLDER_CLASS.PLACEHOLDER_METHOD");
        console.log("[*] arg0 =", safeString(arg0));
        logDivider();

        var result = overload.call(this, arg0);

        console.log("[*] result =", safeString(result));
        logDivider();
        return result;
    };
});
